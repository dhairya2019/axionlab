---
phase: 01-nextjs-migration-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - components/motion/MotionWrapper.tsx
  - components/Nav.tsx
  - components/Footer.tsx
  - components/Chatbot.tsx
  - app/page.tsx
  - app/philosophy/page.tsx
  - app/capabilities/page.tsx
  - app/work/page.tsx
  - app/insights/page.tsx
  - app/careers/page.tsx
  - app/initiate/page.tsx
  - app/not-found.tsx
autonomous: true
requirements:
  - RTG-01
  - RTG-02
  - RTG-03
  - RTG-05
  - RND-01
  - RND-02
  - RND-03

must_haves:
  truths:
    - "All 7 pages render at clean URLs: /, /philosophy, /capabilities, /work, /insights, /careers, /initiate"
    - "Nav shows active state based on usePathname — not window.location.hash"
    - "All nav and footer links use next/link — no hash-prefixed hrefs"
    - "Framer Motion animations work via MotionWrapper without making pages Client Components"
    - "Chatbot has 'use client' directive and renders on every page"
    - "Initiate page has 'use client' for form state management"
    - "Custom 404 page renders in AXIONLAB dark theme"
    - "Static pages (Philosophy, Capabilities, Work, Careers) remain Server Components"
  artifacts:
    - path: "components/motion/MotionWrapper.tsx"
      provides: "'use client' re-export of motion and AnimatePresence from framer-motion"
      exports: ["motion", "AnimatePresence"]
    - path: "components/Nav.tsx"
      provides: "Navigation with usePathname, next/link, 'use client'"
      contains: "usePathname"
    - path: "components/Footer.tsx"
      provides: "Footer with next/link clean URLs"
      contains: "next/link"
    - path: "components/Chatbot.tsx"
      provides: "Chat widget with 'use client' directive"
      contains: "use client"
    - path: "app/not-found.tsx"
      provides: "Custom 404 page in dark theme"
      contains: "Signal Lost"
  key_links:
    - from: "app/page.tsx"
      to: "components/motion/MotionWrapper.tsx"
      via: "import { motion } from '@/components/motion/MotionWrapper'"
      pattern: "import.*MotionWrapper"
    - from: "components/Nav.tsx"
      to: "next/navigation"
      via: "usePathname() for active route detection"
      pattern: "usePathname"
    - from: "components/Nav.tsx"
      to: "next/link"
      via: "Link component for client-side navigation"
      pattern: "import Link from.*next/link"
    - from: "components/Footer.tsx"
      to: "next/link"
      via: "Link component for internal page links"
      pattern: "import Link from.*next/link"
---

<objective>
Migrate all components and pages to work with Next.js App Router: create the Framer Motion 'use client' wrapper, convert Nav to usePathname + next/link, convert Footer to next/link, add 'use client' to Chatbot and Initiate, fix all page imports, and create the custom 404 page.

Purpose: This is the largest plan in Phase 1 — it touches all 7 pages plus 4 components. After this plan, every page renders as the correct component type (Server or Client) with working navigation. The MotionWrapper pattern is critical: it lets page.tsx files stay as Server Components while still using Framer Motion animations.

Output: All pages accessible at clean URLs, navigation works with active state, animations play, 404 page exists.
</objective>

<execution_context>
@/Users/dhairya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dhairya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-nextjs-migration-foundation/01-RESEARCH.md
@.planning/phases/01-nextjs-migration-foundation/01-02-SUMMARY.md

Key patterns from RESEARCH.md:
- MotionWrapper: 'use client' + re-export { motion, AnimatePresence } from 'framer-motion'
- Nav: Replace window.location.hash with usePathname(), replace <a href="#/..."> with <Link href="/...">
- Footer: Replace all href="#/path" with href="/path", use next/link
- Only 3 components need "use client": Nav (useState + usePathname), Chatbot (useState + fetch), Initiate (useState + form)
- Static pages (Philosophy, Capabilities, Work, Careers) must NOT have "use client"

Source files to modify:
@components/Nav.tsx
@components/Footer.tsx
@components/Chatbot.tsx
@app/page.tsx
@app/philosophy/page.tsx
@app/capabilities/page.tsx
@app/work/page.tsx
@app/insights/page.tsx
@app/careers/page.tsx
@app/initiate/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MotionWrapper and add 'use client' to Chatbot</name>
  <files>components/motion/MotionWrapper.tsx, components/Chatbot.tsx</files>
  <action>
**Create `components/motion/MotionWrapper.tsx`:**

Create the directory `components/motion/` if it does not exist. Create `MotionWrapper.tsx` with:

```typescript
'use client'
export { motion, AnimatePresence } from 'framer-motion'
```

This is the thin re-export wrapper pattern from RESEARCH.md. It creates a 'use client' boundary so that Server Component pages can use `motion.div` without becoming Client Components themselves.

**Modify `components/Chatbot.tsx`:**

Add `'use client'` as the VERY FIRST LINE of the file (before any imports). Chatbot uses useState, useEffect, useRef, and fetch — all of which require a Client Component.

Do NOT change any other logic in Chatbot.tsx. The component already works correctly — it just needs the directive.

Verify Chatbot uses named export: `export const Chatbot: React.FC` — this matches the import in layout.tsx (`import { Chatbot } from '@/components/Chatbot'`).
  </action>
  <verify>
    <automated>cd /Users/dhairya/Documents/GitHub/axionlab && test -f components/motion/MotionWrapper.tsx && grep -c "'use client'" components/motion/MotionWrapper.tsx && head -1 components/Chatbot.tsx | grep -c "use client" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>MotionWrapper.tsx exists at components/motion/ with 'use client' re-exporting motion and AnimatePresence; Chatbot.tsx has 'use client' as first line</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Nav.tsx and Footer.tsx to next/link + usePathname</name>
  <files>components/Nav.tsx, components/Footer.tsx</files>
  <action>
**Modify `components/Nav.tsx`:**

Read the current file first to understand the full structure. Then make these specific changes:

1. **Add/ensure `'use client'` directive** as the first line (Nav already needs it for useState/useEffect — just ensure it is present).

2. **Add imports:**
   ```typescript
   import Link from 'next/link'
   import { usePathname } from 'next/navigation'
   ```

3. **Replace hash-based pathname detection:**
   - REMOVE the `useState(() => window.location.hash.replace(...))` for pathname
   - REMOVE the `useEffect` with `window.addEventListener("hashchange", ...)` listener
   - REPLACE with: `const pathname = usePathname()`

4. **Replace all `<a>` tags with `<Link>` for internal links:**
   - `<a href="#/">` → `<Link href="/">`
   - `<a href="#/philosophy">` → `<Link href="/philosophy">`
   - `<a href="#/capabilities">` → `<Link href="/capabilities">`
   - `<a href="#/work">` → `<Link href="/work">`
   - `<a href="#/insights">` → `<Link href="/insights">`
   - `<a href="#/initiate">` → `<Link href="/initiate">`
   - `<a href="#/careers">` → `<Link href="/careers">`
   Leave external links (`mailto:`, `https://`) as regular `<a>` tags.

5. **Update active link detection:**
   - Before: `pathname === link.href.replace(/^#/, '')` or similar hash comparison
   - After: `pathname === link.href`

6. **Update LINKS array** (if it exists): Remove `#` prefix from all href values:
   - `{ name: "Philosophy", href: "#/philosophy" }` → `{ name: "Philosophy", href: "/philosophy" }`

7. **Remove mobile menu close on hashchange** — instead, close mobile menu when pathname changes (add useEffect watching pathname).

Keep the scroll listener (controls nav background styling) — it is still needed.

**Modify `components/Footer.tsx`:**

Read the current file first. Then:

1. **Add import:** `import Link from 'next/link'`

2. **Replace all hash-prefixed internal links:**
   - `<a href="#/philosophy">` → `<Link href="/philosophy">`
   - `<a href="#/capabilities">` → `<Link href="/capabilities">`
   - `<a href="#/work">` → `<Link href="/work">`
   - `<a href="#/insights">` → `<Link href="/insights">`
   - `<a href="#/careers">` → `<Link href="/careers">`
   - `<a href="#/initiate">` → `<Link href="/initiate">`
   Leave external links (`mailto:`, `https://calendly...`, etc.) as `<a>` tags.

3. Footer does NOT need `'use client'` — it has no state, no hooks, no browser APIs. It is a Server Component. The `Link` component from next/link works in Server Components.
  </action>
  <verify>
    <automated>cd /Users/dhairya/Documents/GitHub/axionlab && grep -c "usePathname" components/Nav.tsx && grep -c "next/link" components/Nav.tsx && grep -c "next/link" components/Footer.tsx && ! grep -q 'href="#/' components/Nav.tsx && ! grep -q 'href="#/' components/Footer.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Nav.tsx uses usePathname() from next/navigation, all internal links use next/link with clean paths (no #/ prefixes), hashchange listener removed; Footer.tsx uses next/link for all internal links with clean paths, no 'use client' directive</done>
</task>

<task type="auto">
  <name>Task 3: Fix all page components and create 404 page</name>
  <files>app/page.tsx, app/philosophy/page.tsx, app/capabilities/page.tsx, app/work/page.tsx, app/insights/page.tsx, app/careers/page.tsx, app/initiate/page.tsx, app/not-found.tsx</files>
  <action>
Read each page file first to understand what it currently does. Then apply these changes:

**app/page.tsx (Home):**
- Change `import { motion } from "framer-motion"` to `import { motion } from '@/components/motion/MotionWrapper'`
- Do NOT add 'use client' — the MotionWrapper handles the client boundary
- Replace any `<a href="#/...">` with `<Link href="/...">` (add `import Link from 'next/link'` if needed)
- If the file uses `AnimatePresence`, import it from MotionWrapper too

**app/philosophy/page.tsx, app/capabilities/page.tsx, app/work/page.tsx, app/careers/page.tsx:**
- If the file imports `motion` from `framer-motion`, change to import from `'@/components/motion/MotionWrapper'`
- Replace any `<a href="#/...">` with `<Link href="/...">` (add Link import)
- Do NOT add 'use client' — these are Server Components (RND-01)
- If the page does NOT use motion, no import change needed

**app/insights/page.tsx:**
- Same treatment as static pages above
- Do NOT add MDX or blog infrastructure — that is Phase 2 scope
- Just ensure it renders at /insights without errors

**app/initiate/page.tsx:**
- Add `'use client'` as the FIRST LINE — this page has form state (useState for form data and submission status)
- Replace any `<a href="#/...">` with `<Link href="/...">` (add Link import)
- If it imports motion from framer-motion, change to import from MotionWrapper

**app/not-found.tsx (NEW file):**
Create `app/not-found.tsx` with:

```typescript
import Link from 'next/link'

export default function NotFound() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center px-6">
      <div className="max-w-xl">
        <span className="font-mono text-accent text-[10px] tracking-[0.4em] uppercase font-black">
          404 — Node Not Found
        </span>
        <h1 className="text-8xl font-black uppercase tracking-tighter text-white mt-4 mb-8">
          Signal Lost.
        </h1>
        <p className="text-muted uppercase tracking-tight font-bold mb-12">
          The path you requested does not exist in this system.
        </p>
        <Link
          href="/"
          className="px-10 h-14 bg-accent text-white inline-flex items-center justify-center text-[12px] font-black uppercase tracking-[0.3em] hover:bg-white hover:text-black transition-all"
        >
          Return to Base
        </Link>
      </div>
    </div>
  )
}
```

This is a Server Component (no 'use client' needed). It uses the design system tokens: bg-background (#080808), text-accent (#ff1f3d), text-muted (#666666).

**For ALL pages:**
- Ensure no page imports from `./components/...` — use `@/components/...` instead (path alias from tsconfig)
- Do NOT add metadata exports yet — per-page SEO metadata is Phase 3 scope. The root layout provides a default title.
- Do NOT modify any visual content, text, or styling — migration only
  </action>
  <verify>
    <automated>cd /Users/dhairya/Documents/GitHub/axionlab && test -f app/not-found.tsx && ! grep -q "from ['\"]framer-motion['\"]" app/page.tsx && ! grep -q 'href="#/' app/page.tsx app/philosophy/page.tsx app/capabilities/page.tsx app/work/page.tsx app/insights/page.tsx app/careers/page.tsx app/initiate/page.tsx 2>/dev/null && head -1 app/initiate/page.tsx | grep -q "use client" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 7 pages have hash hrefs replaced with clean paths; pages using motion import from MotionWrapper instead of framer-motion; app/initiate/page.tsx has 'use client'; static pages do NOT have 'use client'; app/not-found.tsx exists with dark theme 404 design</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `test -f components/motion/MotionWrapper.tsx` passes
2. `grep "'use client'" components/motion/MotionWrapper.tsx` finds directive
3. `grep "usePathname" components/Nav.tsx` finds hook usage
4. `grep -r 'href="#/' components/ app/` finds ZERO hash-prefixed links
5. `grep -r "from ['\"]framer-motion['\"]" app/` finds ZERO direct framer-motion imports in pages
6. `head -1 app/initiate/page.tsx` shows 'use client'
7. `head -1 app/philosophy/page.tsx` does NOT show 'use client'
8. `test -f app/not-found.tsx` passes
9. `grep "Signal Lost" app/not-found.tsx` finds the 404 heading
</verification>

<success_criteria>
- components/motion/MotionWrapper.tsx exists with 'use client' + re-exports motion and AnimatePresence
- components/Chatbot.tsx has 'use client' as first line
- components/Nav.tsx uses usePathname() from next/navigation and Link from next/link — no window.location.hash
- components/Footer.tsx uses Link from next/link — no hash-prefixed hrefs
- All 7 page files have zero `href="#/"` references
- No page.tsx file in app/ imports directly from 'framer-motion' — all use MotionWrapper
- app/initiate/page.tsx has 'use client'; static pages (philosophy, capabilities, work, careers) do NOT
- app/not-found.tsx exists with AXIONLAB dark theme design
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextjs-migration-foundation/01-04-SUMMARY.md`
</output>
